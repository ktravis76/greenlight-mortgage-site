# Claude Code — Greenlight Mortgage build

Open Terminal, `cd` into the repo, run `claude`, and paste the block below.

**Repo:**
```
cd ~/Library/Application\ Support/Claude/local-agent-mode-sessions/9beb94a4-ab9b-453a-b6d1-d9214cb54051/97d303ef-f3f9-40bc-805a-87d80ba96ff2/local_c870f81a-b04b-4a1f-8782-ac6ead633b5b/outputs/repos/greenlight-mortgage-site
```

Move it somewhere permanent first if you like — `~/Projects/greenlight` is fine. Git history comes with it.

---

## PASTE THIS

```
You are finishing the Greenlight Mortgage website. Read MIGRATION-MAP.md and README.md in this
repo before writing anything — they hold the page map and the compliance rules.

## THE BAR

Greenlight is a mortgage brokerage in Longview, East Texas, owned by Kenneth Travis. Their
current site is a rented WordPress template on a vendor's multisite. It is dated and generic.

The site you build has to be OBVIOUSLY better than what they have — not "cleaner," obviously
better, to a non-technical owner looking at it for ten seconds. If it looks like a competent
Bootstrap template you have failed. Real visual craft: considered type scale, real whitespace,
motion that earns its place, photography treated as a design element, a homepage that looks
like a company that is winning.

Benchmark against Guild Mortgage, Fairway, and Better.com — then be warmer and more local
than any of them. This is East Texas, not Silicon Valley.

## STACK

Static HTML/CSS/vanilla JS. No framework, no build step beyond the existing Python generator.
Deploys to Vercel. Supabase for data. Keep it fast — the current site is slow and that is part
of the win.

Loan pages are generated by `build-loans.py`. Extend that pattern for other repeated page
types rather than hand-writing near-identical files. Run `python3 build-loans.py` after edits.

Local preview: `python3 -m http.server 8787` then http://127.0.0.1:8787

## WHAT ALREADY EXISTS

- `index.html` — homepage recreation. Structure right, visual execution weak. REDESIGN IT.
- `loans/` — 6 program pages + hub, generated. Content is solid, design needs the same lift.
- `tools/va-refi-screener.html` — internal sales tool. Working, polished, leave it alone.
- `assets/from-old-site/` — kt-video.mp4 (17MB), headshot, both logos, NMLS + EHL badges.
  These were rescued off the vendor's server. They are the only copy. Do not delete.
- `style.css` — shared stylesheet.
- `MIGRATION-MAP.md` — all 21 pages with their 301 targets.

## BUILD THESE (13 pages missing)

About (Meet the Team) · Reviews · Testimonials · Contact · Blog + post template ·
Learning Center · Resources · Mortgage Calculator · Home Value · Privacy Policy ·
Accessibility · Survey · Tools hub

Plus the tools:

1. **Estimated Savings** at /tools/estimate — THE primary CTA, currently linked from every
   loan page and 404ing. Consumer-facing version of the internal screener.
   GATED: name, phone, email required to see results. Disclose the ask BEFORE they start,
   collect it AFTER they answer the loan questions, three fields only, show the result on
   screen AND email it.
   NEVER call it a "quote" to consumers — only a licensed LO can quote. Use "Estimated
   Savings" / "See What You Could Save."
   TCPA: separate un-ticked consent checkbox naming Greenlight, covering autodialed calls and
   texts, stating consent is NOT a condition of obtaining a loan, with opt-out. Log the
   consent text, timestamp, IP, and user agent.
2. **Mortgage Calculator** — payment breakdown, amortization, extra-payment scenarios
3. **Home Value** — lead capture form
4. Partner tools (later phase): buyer affordability, seller net proceeds, rent vs buy

## SUPABASE — already built, do not recreate

Project `athovwknbwbbqworsbrm`. Tables that exist with RLS enabled:

- `leads` — name, email, phone, state, source, goal, loan_type, veteran, current_rate,
  current_payment, mortgage_balance, estimated_home_value, total_debt,
  estimated_monthly_savings, priority, status, statement_uploaded, tcpa_consent, consent_at,
  consent_ip, consent_ua, notes
- `mortgage_statement_uploads` — storage_path, original_filename, mime_type, status, lead_id FK
- `soft_quotes` — 41 cols, the quote engine. Screener already writes here.
- `profiles` — staff, FK to auth.users, role admin|loan_officer
- `allowed_staff` — email allowlist for staff signup
- `analytics_events` — event + jsonb params
- `ct_events` / `ct_rsvps` — The Closing Table event RSVPs

CRITICAL: every table has RLS on and ZERO rows. No policy has ever faced real traffic.
AUDIT THE RLS POLICIES before any borrower data lands. A bad policy on a table holding
consumer contact data and TCPA consent is the worst outcome available here.

Forms post directly to Supabase, then forward to GoHighLevel/Konnectd
(location LRAzmr7bQKMMlXiYfvi6). Do NOT email a person — leads currently land in individual
inboxes and get reconciled by hand. That is the problem this fixes.

## COMPLIANCE — non-negotiable, every consumer page

- "Powered by Co/LAB Lending"
- Company NMLS 2426021 · Kenneth Travis individual NMLS 233918
- State licenses (CONFIRMED by KT, 5 states): TX 2426021 · AL 23417 · LA 2426021 · ND ML104832 · MI (license # still needed)
  Florida and South Carolina are NOT held. Never list them.
- Equal Housing Opportunity
- "This is not a commitment to lend"
- "Subject to credit approval and underwriting" beside ANY number
- No stated rates, APRs, payments, or approval guarantees anywhere
- Rate quotes and eligibility deferred to licensed loan officers

HOW it appears matters as much as that it appears. Footer carries the weight. One short line
beside a number, never a paragraph. EHO and NMLS as clean trust marks in the design, the way
a bank shows FDIC. No consent gates or interstitials. Minimum 12px with real contrast.

Two things on the OLD site that must NOT carry forward:
1. A Reg Z "business professionals only" disclaimer on a consumer page that says "Dear Future
   Homeowner." Contradictory. Rewrite.
2. "I guarantee" in Kenneth's welcome letter. Rephrase.

## VERIFIED FACTS — use these, invent nothing

Greenlight Mortgage, LLC · founded 2008 · 4523 Judson Rd, Longview, TX 75605 · 903-331-0892
Kenneth Travis, President-CEO, NMLS 233918. USMC 8 years, Sergeant. Licensed in 5 states.
Tagline: "Green means GO! Drive home with Greenlight Mortgage."
Core Values: Service & Compassion · Operational Excellence · Trust & Integrity
Kenneth's line: "how can we be better, how can we be different?"
Ranks #2 organically for "mortgage longview tx" — protect this.
Applications go to https://greenlight.my1003app.com/233918/register — an external licensed
LOS. DO NOT rebuild the 1003. It collects SSNs. Build a short pre-app intake in front of it.

Testimonials (verbatim, already in index.html): Tim/Longview, Jason/Gilmer, Maxwell/Jefferson,
John/Longview, Robyn/Longview, Jasper/Longview, Brent/Longview.

Team roster and individual LO NMLS numbers are NOT confirmed — use placeholders clearly
marked, do not invent people.

## SEO

Kenneth's own June audit: the current title tag is keyword-stuffed and a penalty risk, only
~10 pages are indexed vs competitors at 50+, and the blog is nearly empty.
Target local intent: Longview, Gilmer, Kilgore, Hallsville, White Oak, Tyler, Marshall,
Jefferson, plus Spring Hill / Pine Tree / Hallsville school districts.
One primary phrase per page. Schema: MortgageBroker, FAQPage, Article, BreadcrumbList.
Sitemap.xml and robots.txt.

## PHASE 2 — after the marketing site is genuinely excellent

Client portal at /portal: Supabase Auth email OTP (no passwords for borrowers), loan status
timeline, document upload against mortgage_statement_uploads, messages with their LO.
Back office at /admin: lead inbox, pipeline, file-scoped comments with @mentions for Kenneth,
Julia, Lisa, Kimberly, Jared, Preston, Ryan. Resend for transactional email.
Do NOT build payments — RESPA and trust-account rules, needs compliance sign-off first.
Do NOT rebuild GoHighLevel — 7,863 contacts live there and it stays system of record.

## WORKING RULES

- Commit after each page with a real message
- Verify every internal link resolves before saying done — a previous pass shipped nav
  pointing at /loans/va while files were at /loans/va.html and every link 404'd
- Run a compliance grep after each build: confirm zero hits for "guaranteed", "lowest rate",
  "best rate", and any APR figure
- Mark missing facts visibly on the page rather than inventing them
- Mobile first, real device widths, accessible contrast and focus states

## DO FIRST

1. Look at index.html and the loan pages in the browser
2. Redesign the visual layer until it is unmistakably better than a template
3. Then build the 13 missing pages
4. Then the Estimated Savings tool
5. Then phase 2

Do not tell me it is done until you have clicked every link.
```

---

## After it builds

The GitHub repo `github.com/ktravis76/GreenlightMortgage` exists and is empty. Local git has
the full history. One push from the repo folder connects them and Vercel deploys on every
commit after that:

```
git push -u origin main
```

Paste the token as the password when git prompts. macOS Keychain stores it — the credential
helper is already configured, so it only happens once.

Domain (`greenlightmortgage.com`, GoDaddy, currently pointed at Cloudflare nameservers) stays
untouched until Kenneth signs off on the finished site.
